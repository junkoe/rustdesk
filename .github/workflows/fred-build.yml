# fred-portal-build.yml
# Place this file in your RustDesk fork at: .github/workflows/fred-portal-build.yml
#
# Builds "Fred Portal" — the bidirectional portal client for MSP end-users.
# Key differences from fred-admin-build.yml:
#   - Phones home (register + heartbeat) with client_type:"portal" — no baked-in password
#   - Polling thread uses /api/portal/auth + portal_client_token (not /api/auth + portal_token)
#   - Simpler right panel: title + _FsPortalWidget only (no alias, quick-connect, scratch pad)
#   - Settings: General + About only
#   - No remote_page.dart patch (no session tracking)
#
# Trigger: manually via Actions → "Run workflow" only (portal client is distributed manually)
# Runner:  windows-2022 (free for public repos)
# Output:  Fred_Portal.exe  (GitHub Actions artifact, 90-day retention)

name: Build Fred Portal

on:
  workflow_dispatch:

env:
  RUST_VERSION:    "1.75.0"
  FLUTTER_VERSION: "3.24.5"
  LLVM_VERSION:    "15.0.6"
  VCPKG_COMMIT:    "120deac3062162151622ca4860575a33844ba10b"
  PORTAL_URL:      "https://help.fredtheitguy.com"
  APP_NAME:        "FRDP Portal"
  APP_TITLE:       "FRDP Portal"

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 150

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - name: Checkout fork with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      # ── Cache: vcpkg binary cache ──────────────────────────────────────────
      - name: Cache vcpkg binary cache
        uses: actions/cache@v4
        with:
          path: C:\Users\runneradmin\AppData\Local\vcpkg\archives
          key: vcpkg-${{ env.VCPKG_COMMIT }}-x64-windows-static
          restore-keys: vcpkg-${{ env.VCPKG_COMMIT }}-

      # ── Cache: Rust registry + build cache ────────────────────────────────
      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: cargo-${{ env.RUST_VERSION }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-${{ env.RUST_VERSION }}-

      # ── Rust ────────────────────────────────────────────────────────────────
      - name: Install Rust ${{ env.RUST_VERSION }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: x86_64-pc-windows-msvc
          components: rustfmt

      # ── LLVM ────────────────────────────────────────────────────────────────
      - name: Install LLVM ${{ env.LLVM_VERSION }}
        uses: KyleMayes/install-llvm-action@v1
        with:
          version: ${{ env.LLVM_VERSION }}
          directory: C:\llvm

      - name: Set LIBCLANG_PATH
        run: echo "LIBCLANG_PATH=C:\llvm\bin" >> $env:GITHUB_ENV

      # ── Flutter ─────────────────────────────────────────────────────────────
      - name: Install Flutter ${{ env.FLUTTER_VERSION }}
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Disable Flutter analytics
        run: flutter config --no-analytics

      - name: Flutter precache (Windows)
        run: flutter precache --windows

      # ── Custom Flutter engine (required for RustDesk) ─────────────────────
      - name: Download RustDesk custom Flutter engine
        run: |
          Invoke-WebRequest `
            -Uri "https://github.com/rustdesk/engine/releases/download/main/windows-x64-release.zip" `
            -OutFile "$env:TEMP\rd-engine.zip" `
            -UseBasicParsing

      - name: Install custom Flutter engine
        run: |
          $flutterRoot  = (Get-Command flutter).Source | Split-Path | Split-Path
          $engineDest   = "$flutterRoot\bin\cache\artifacts\engine\windows-x64-release"
          New-Item -ItemType Directory -Force -Path $engineDest | Out-Null
          Expand-Archive -Path "$env:TEMP\rd-engine.zip" -DestinationPath "$env:TEMP\rd-engine" -Force
          Copy-Item -Path "$env:TEMP\rd-engine\*" -Destination $engineDest -Force
          Write-Host "Custom engine installed to $engineDest"

      # ── Flutter dropdown-menu patch (3.24.4/3.24.5) ───────────────────────
      - name: Apply Flutter dropdown patch
        run: |
          $patch = ".github\patches\flutter_3.24.4_dropdown_menu_enableFilter.diff"
          if (Test-Path $patch) {
            $flutterRoot = (Get-Command flutter).Source | Split-Path | Split-Path
            Copy-Item $patch $flutterRoot
            Push-Location $flutterRoot
            git apply "flutter_3.24.4_dropdown_menu_enableFilter.diff" 2>$null || true
            Pop-Location
            Write-Host "Flutter patch applied"
          } else {
            Write-Host "Patch file not found — skipping"
          }

      # ── vcpkg ───────────────────────────────────────────────────────────────
      - name: Setup vcpkg at pinned commit
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT }}
          vcpkgDirectory: C:\vcpkg

      - name: Install vcpkg dependencies (x64-windows-static)
        run: |
          C:\vcpkg\vcpkg install `
            --triplet x64-windows-static `
            --x-install-root="C:\vcpkg\installed"
        env:
          VCPKG_ROOT: C:\vcpkg
          VCPKG_DEFAULT_HOST_TRIPLET: x64-windows-static

      # ── Add ureq HTTP client ─────────────────────────────────────────────
      - name: Add ureq to Cargo.toml
        shell: pwsh
        run: |
          $cargo = Get-Content "Cargo.toml" -Raw
          if ($cargo -notmatch '"ureq"') {
            $cargo = $cargo -replace '(\[dependencies\])', "[dependencies]`nureq = { version = `"2`", features = [`"tls`"] }"
            $cargo | Set-Content "Cargo.toml" -Encoding utf8
            Write-Host "Added ureq to Cargo.toml"
          } else {
            Write-Host "ureq already present"
          }

      # ── Patch src/common.rs ──────────────────────────────────────────────────
      # Combines admin client (relay + frdp module + polling thread) with
      # customer client (phone-home register + heartbeat), adapted for portal:
      #   - client_type:"portal" in registration body
      #   - No baked-in password — server delivers via heartbeat as with support clients
      #   - Polling thread uses portal_client_token + /api/portal/auth
      - name: Patch src/common.rs (relay + phone-home + portal polling)
        shell: pwsh
        env:
          RUSTDESK_HOST: ${{ secrets.RUSTDESK_HOST }}
          RUSTDESK_KEY:  ${{ secrets.RUSTDESK_KEY }}
        run: |
          $h = $env:RUSTDESK_HOST
          $k = $env:RUSTDESK_KEY
          $p = $env:PORTAL_URL
          $f = "src\common.rs"
          $c = Get-Content $f -Raw
          $before = $c

          $inject = @"

              // ── FRDP Portal: compiled-in relay server ────────────────────────────
              *hbb_common::config::EXE_RENDEZVOUS_SERVER.write().unwrap() = "$h".to_owned();
              if !"$k".is_empty() {
                  hbb_common::config::Config::set_option("key".to_owned(), "$k".to_owned());
              }
              // ── FRDP Portal: allow remote config modification (admin support) ────
              hbb_common::config::Config::set_option("allow-remote-config-modification".to_owned(), "Y".to_owned());
              // ── FRDP Portal: disable update check notifications ──────────────────
              hbb_common::config::Config::set_option("enable-check-update".to_owned(), "N".to_owned());
              // ── FRDP Portal: force dark theme ────────────────────────────────────
              hbb_common::config::LocalConfig::set_option("theme".to_owned(), "dark".to_owned());
              // ── FRDP Portal: phone-home registration (client_type = portal) ──────
              // No baked-in password — server assigns via heartbeat response.
              std::thread::spawn(|| {
                  std::thread::sleep(std::time::Duration::from_secs(10));
                  let id = hbb_common::config::Config::get_id();
                  if id.is_empty() { return; }
                  let machine = std::env::var("COMPUTERNAME").unwrap_or_else(|_| "Unknown".to_owned());
                  let id_c   = id.replace('"', "");
                  let name_c = machine.replace('"', "").replace('\\', "");
                  let body   = format!(
                      r#"{{"client_id":"{}","machine_name":"{}","code":"","client_type":"portal"}}"#,
                      id_c, name_c
                  );
                  for _ in 0..3u8 {
                      if let Ok(resp) = ureq::post(&format!("{}/api/register", "$p"))
                          .set("Content-Type", "application/json")
                          .timeout(std::time::Duration::from_secs(10))
                          .send_string(&body) {
                          if resp.status() < 400 { break; }
                      }
                      std::thread::sleep(std::time::Duration::from_secs(5));
                  }
                  // Heartbeat loop — keeps status online, receives server-pushed passwords
                  let hb_body = format!(r#"{{"client_id":"{}"}}"#, id_c);
                  loop {
                      std::thread::sleep(std::time::Duration::from_secs(60));
                      if let Ok(resp) = ureq::post(&format!("{}/api/heartbeat", "$p"))
                          .set("Content-Type", "application/json")
                          .timeout(std::time::Duration::from_secs(10))
                          .send_string(&hb_body)
                      {
                          if let Ok(hb_resp) = resp.into_string() {
                              let needle = "\"new_password\":\"";
                              if let Some(start) = hb_resp.find(needle) {
                                  let after = &hb_resp[start + needle.len()..];
                                  if let Some(end) = after.find('"') {
                                      let pw = &after[..end];
                                      if !pw.is_empty() {
                                          hbb_common::config::Config::set_permanent_password(pw);
                                          let confirm = format!(r#"{{"client_id":"{}"}}"#, id_c);
                                          let _ = ureq::post(&format!("{}/api/device/password-applied", "$p"))
                                              .set("Content-Type", "application/json")
                                              .timeout(std::time::Duration::from_secs(10))
                                              .send_string(&confirm);
                                      }
                                  }
                              }
                          }
                      }
                  }
              });
              // ── FRDP Portal: connect request polling (portal_client_token) ───────
              {
                  let _pu = "$p".to_owned();
                  std::thread::spawn(move || {
                      let _tk = "portal_client_token";
                      loop {
                          std::thread::sleep(std::time::Duration::from_secs(10));
                          let _tok = hbb_common::config::LocalConfig::get_option(_tk);
                          if _tok.is_empty() { continue; }
                          let _body = format!(r#"{{"token":"{}"}}"#, _tok);
                          match ureq::post(&format!("{}/api/poll", _pu))
                              .set("Content-Type", "application/json")
                              .timeout(std::time::Duration::from_secs(8))
                              .send_string(&_body)
                          {
                              Ok(_resp) => {
                                  if let Ok(_text) = _resp.into_string() {
                                      let _req_id: Option<u64> = (|| {
                                          let _pos = _text.find("\"id\":")?;
                                          let _rest = _text[_pos + 5..].trim_start();
                                          let _end = _rest.find(|c: char| !c.is_ascii_digit()).unwrap_or(_rest.len());
                                          if _end == 0 { return None; }
                                          _rest[.._end].parse().ok()
                                      })();
                                      if let Some(_rid) = _req_id {
                                          let _cb = format!(r#"{{"token":"{}"}}"#, _tok);
                                          if let Ok(_cr) = ureq::post(&format!("{}/api/connect/{}/credentials", _pu, _rid))
                                              .set("Content-Type", "application/json")
                                              .timeout(std::time::Duration::from_secs(8))
                                              .send_string(&_cb)
                                          {
                                              if let Ok(_creds) = _cr.into_string() {
                                                  let _pk = "\"peer_id\":\"";
                                                  let _wk = "\"password\":\"";
                                                  let _mk = "\"machine_name\":\"";
                                                  let mut _pid_s = String::new();
                                                  let mut _pw_s  = String::new();
                                                  let mut _mn_s  = String::new();
                                                  if let Some(_i) = _creds.find(_pk) {
                                                      let _s = _i + _pk.len();
                                                      if let Some(_e) = _creds[_s..].find('"') { _pid_s = _creds[_s.._s+_e].to_owned(); }
                                                  }
                                                  if let Some(_i) = _creds.find(_wk) {
                                                      let _s = _i + _wk.len();
                                                      if let Some(_e) = _creds[_s..].find('"') { _pw_s = _creds[_s.._s+_e].to_owned(); }
                                                  }
                                                  if let Some(_i) = _creds.find(_mk) {
                                                      let _s = _i + _mk.len();
                                                      if let Some(_e) = _creds[_s..].find('"') { _mn_s = _creds[_s.._s+_e].to_owned(); }
                                                  }
                                                  if !_pid_s.is_empty() && !_pw_s.is_empty() {
                                                      frdp::set_pw(&_pid_s, _pw_s);
                                                  }
                                                  let _pending = format!(r#"{{"peer_id":"{}","machine_name":"{}"}}"#, _pid_s, _mn_s);
                                                  hbb_common::config::LocalConfig::set_option(
                                                      "frdp_pending_connect".to_owned(),
                                                      _pending,
                                                  );
                                              }
                                          }
                                      }
                                  }
                              }
                              Err(ureq::Error::Status(401, _)) => {
                                  // Token expired — clear so Flutter shows re-auth form
                                  hbb_common::config::LocalConfig::set_option(_tk.to_owned(), String::new());
                              }
                              Err(_) => {}
                          }
                      }
                  });
              }
          "@

          # Module-level frdp helper injected before load_custom_client()
          $mod_inject = @"

              // -- FRDP: in-memory password store (never written to disk) ----------
              pub mod frdp {
                  static _PW_MAP: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<String, String>>> =
                      std::sync::OnceLock::new();
                  pub fn set_pw(peer_id: &str, password: String) {
                      _PW_MAP.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
                          .lock().unwrap()
                          .insert(peer_id.to_owned(), password);
                  }
                  pub fn take_pw(peer_id: &str) -> String {
                      _PW_MAP.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
                          .lock().unwrap()
                          .remove(peer_id)
                          .unwrap_or_default()
                  }
              }
              // ------------------------------------------------------------------

          "@
          $target = [regex]::Match($c, 'pub fn load_custom_client\(\)[^{]*\{').Value
          if (!$target) {
            Write-Error "load_custom_client() not found in $f — function may have been renamed"
            exit 1
          }
          $c = $c.Replace($target, $mod_inject + $target + $inject)
          $c | Set-Content $f -Encoding utf8

          if ($c -eq $before) {
            Write-Error "src/common.rs was not modified"
            exit 1
          }
          Write-Host "Patched load_custom_client() in src/common.rs"
          Write-Host "  EXE_RENDEZVOUS_SERVER = $h"
          Write-Host "  PORTAL_URL            = $p"

      # ── Patch src/client.rs: inject FRDP in-memory password into handle_hash ──
      # Identical to admin client — needed so admin-initiated connections to this
      # portal device auto-fill the server-managed password without writing to disk.
      - name: Patch src/client.rs (FRDP in-memory password in handle_hash)
        shell: pwsh
        run: |
          $f = "src\client.rs"
          $c = Get-Content $f -Raw
          $before = $c

          $match = [regex]::Match($c, '    // last password\r?\n    let mut password = lc\.read\(\)\.unwrap\(\)\.password\.clone\(\);')
          if (!$match.Success) {
            Write-Error "handle_hash '// last password' block not found in src/client.rs — function may have changed"
            exit 1
          }
          $target = $match.Value

          $inject = @"
              // -- FRDP: in-memory password (atomic take, hashed with live salt) --
              {
                  let _pid = lc.read().unwrap().id.clone();
                  let _frdp_pw = crate::common::frdp::take_pw(&_pid);
                  if !_frdp_pw.is_empty() {
                      let mut _sha = Sha256::new();
                      _sha.update(_frdp_pw.as_bytes());
                      _sha.update(&hash.salt);
                      let _res = _sha.finalize();
                      lc.write().unwrap().password = _res[..].into();
                  }
              }
              // -- FRDP end -------------------------------------------------------
          "@

          $c = $c.Replace($target, $inject + "`n" + $target)
          $c | Set-Content $f -Encoding utf8

          if ($c -eq $before) {
            Write-Error "src/client.rs was not modified"
            exit 1
          }
          Write-Host "Patched handle_hash() in src/client.rs"

      # ── Generate bridge_generated.rs ─────────────────────────────────────
      - name: Flutter pub get (required before bridge codegen)
        run: flutter pub get
        working-directory: flutter

      - name: Generate flutter_rust_bridge
        shell: pwsh
        env:
          LIBCLANG_PATH: C:\llvm\bin
        run: |
          cargo install flutter_rust_bridge_codegen --version 1.80.1 --features "uuid" --locked
          flutter_rust_bridge_codegen `
            --rust-input src/flutter_ffi.rs `
            --dart-output flutter/lib/generated_bridge.dart
          Write-Host "bridge_generated.rs created"

      # ── Python build deps ─────────────────────────────────────────────────
      - name: Install Python build dependencies
        run: python -m pip install --quiet requests argparse

      # ── Branding + right panel ────────────────────────────────────────────
      - name: Apply branding and portal right panel
        shell: pwsh
        run: |
          $name  = "${{ env.APP_NAME }}"
          $title = "${{ env.APP_TITLE }}"

          # 1. Window title
          $f = "flutter\windows\runner\main.cpp"
          (Get-Content $f -Raw) -replace 'std::wstring app_name = L"RustDesk"', "std::wstring app_name = L`"$title`"" |
            Set-Content $f -Encoding utf8
          Write-Host "main.cpp: window title → $title"

          # 2. Exe metadata
          $f = "flutter\windows\runner\Runner.rc"
          $bytes = [System.IO.File]::ReadAllBytes($f)
          $enc = if ($bytes[0] -eq 0xFF -and $bytes[1] -eq 0xFE) { [System.Text.Encoding]::Unicode } else { [System.Text.Encoding]::UTF8 }
          $c = $enc.GetString($bytes)
          $c = $c -replace 'VALUE "FileDescription", "RustDesk[^"]*"', "VALUE `"FileDescription`", `"$name`""
          $c = $c -replace 'VALUE "ProductName", "RustDesk[^"]*"',     "VALUE `"ProductName`", `"$name`""
          [System.IO.File]::WriteAllText($f, $c, $enc)
          Write-Host "Runner.rc: product name → $name"

          # 3. Hide update card + inject portal right panel (simpler than admin — no alias/scratchpad)
          $f = "flutter\lib\desktop\pages\desktop_home_page.dart"
          $c = Get-Content $f -Raw
          $c = $c -replace 'bind\.mainGetNewVersion\(\)\.isNotEmpty', 'false'

          $panel = @(
            'Expanded(',
            '              child: Container(',
            '                decoration: BoxDecoration(',
            '                  color: Color(0xFF242424),',
            '                  border: Border(left: BorderSide(color: Color(0xFF2D2D2D))),',
            '                ),',
            '                padding: EdgeInsets.all(20),',
            '                child: Column(',
            '                  crossAxisAlignment: CrossAxisAlignment.start,',
            '                  children: [',
            '                    Row(children: [',
            '                      Icon(Icons.shield_outlined, color: Color(0xFF6366F1), size: 18),',
            '                      SizedBox(width: 8),',
            "                      Text('Fred IT Portal', style: TextStyle(fontWeight: FontWeight.bold, fontSize: 14, color: Color(0xFFF1F5F9))),",
            '                    ]),',
            '                    SizedBox(height: 16),',
            '                    _FsPortalWidget(),',
            '                    SizedBox(height: 12),',
            '                    Divider(color: Color(0xFF2D2D2D)),',
            '                    SizedBox(height: 8),',
            "                    Text('Visit the portal in your browser to manage devices and tickets.',",
            "                      style: TextStyle(fontSize: 11, color: Color(0xFF64748B)),",
            '                    ),',
            '                  ],',
            '                ),',
            '              ),',
            '            )'
          ) -join "`n"

          $c = $c.Replace('if (!isIncomingOnly) Expanded(child: buildRightPane(context))', $panel)
          $c | Set-Content $f -Encoding utf8
          Write-Host "desktop_home_page.dart: update card hidden + portal panel injected"

          # 4. Icons
          if (Test-Path "assets\app_icon.ico") {
            Copy-Item "assets\app_icon.ico" "flutter\windows\runner\resources\app_icon.ico" -Force
            Copy-Item "assets\app_icon.ico" "res\icon.ico" -Force
            Write-Host "Icon: replaced"
          }
          if (Test-Path "assets\polished_logo.svg") {
            Copy-Item "assets\polished_logo.svg" "flutter\assets\icon.svg" -Force
            Write-Host "SVG icon: replaced"
          }

      # ── Inject _FsPortalWidget (portal auth — /api/portal/auth + portal_client_token) ──
      # Identical logic to admin _FsPortalWidget but uses portal endpoint and token key.
      # Security model: same as admin — token-only in LocalConfig, no password stored.
      - name: Inject _FsPortalWidget
        shell: pwsh
        run: |
          $portal = $env:PORTAL_URL
          $f = "flutter\lib\desktop\pages\desktop_home_page.dart"
          $c = Get-Content $f -Raw

          if ($c -notmatch "import 'dart:async'") {
            $c = "import 'dart:async';`n" + $c
          }
          if ($c -notmatch "import 'dart:convert'") {
            $c = "import 'dart:convert';`n" + $c
          }
          if ($c -notmatch "package:http/http.dart") {
            $c = "import 'package:http/http.dart' as rdHttp;`n" + $c
          }

          $cls = @(
            '',
            'class _FsPortalWidget extends StatefulWidget {',
            '  const _FsPortalWidget({Key? key}) : super(key: key);',
            '  @override',
            '  State<_FsPortalWidget> createState() => _FsPortalWidgetState();',
            '}',
            'class _FsPortalWidgetState extends State<_FsPortalWidget> {',
            '  Timer? _timer;',
            "  String _pendingPeerId = '';",
            "  String _pendingLabel = '';",
            '  bool _showSetup = false;',
            "  String _authError = '';",
            '  bool _authing = false;',
            '  final _userCtrl = TextEditingController();',
            '  final _passCtrl = TextEditingController();',
            '  final _totpCtrl = TextEditingController();',
            '',
            '  @override',
            '  void initState() {',
            '    super.initState();',
            '    _checkToken();',
            '    _timer = Timer.periodic(const Duration(seconds: 5), (_) => _poll());',
            '  }',
            '',
            '  @override',
            '  void dispose() {',
            '    _timer?.cancel();',
            '    _userCtrl.dispose();',
            '    _passCtrl.dispose();',
            '    _totpCtrl.dispose();',
            '    super.dispose();',
            '  }',
            '',
            '  void _checkToken() async {',
            "    final t = await bind.mainGetLocalOption(key: 'portal_client_token');",
            '    if (mounted) setState(() { _showSetup = t.isEmpty; });',
            '  }',
            '',
            '  void _auth() async {',
            '    final u = _userCtrl.text.trim();',
            '    final p = _passCtrl.text;',
            '    if (u.isEmpty || p.isEmpty) return;',
            '    if (mounted) setState(() { _authing = true; _authError = ""; });',
            '    try {',
            "      final body = <String, dynamic>{'username': u, 'password': p};",
            '      final tot = _totpCtrl.text.trim();',
            "      if (tot.isNotEmpty) body['totp_code'] = tot;",
            "      final resp = await rdHttp.post(Uri.parse('$portal/api/portal/auth'),",
            "        headers: {'Content-Type': 'application/json'},",
            '        body: jsonEncode(body),',
            '      );',
            '      if (resp.statusCode == 200) {',
            '        final decoded = jsonDecode(resp.body) as Map<String, dynamic>;',
            "        final tok = decoded['token'] as String?;",
            '        if (tok != null && tok.isNotEmpty) {',
            "          await bind.mainSetLocalOption(key: 'portal_client_token', value: tok);",
            '          _userCtrl.clear(); _passCtrl.clear(); _totpCtrl.clear();',
            '          if (mounted) setState(() { _showSetup = false; _authing = false; });',
            '          return;',
            '        }',
            "        if (mounted) setState(() { _authError = 'Unexpected response.'; _authing = false; });",
            '      } else if (resp.statusCode == 429) {',
            "        if (mounted) setState(() { _authError = 'Too many attempts — try again in 15 min.'; _authing = false; });",
            '      } else {',
            "        if (mounted) setState(() { _authError = 'Invalid credentials or TOTP code.'; _authing = false; });",
            '      }',
            '    } catch (_) {',
            "      if (mounted) setState(() { _authError = 'Connection failed.'; _authing = false; });",
            '    }',
            '  }',
            '',
            '  void _poll() async {',
            "    final tok = await bind.mainGetLocalOption(key: 'portal_client_token');",
            '    if (tok.isEmpty) {',
            '      if (!_showSetup && mounted) setState(() { _showSetup = true; });',
            '      return;',
            '    }',
            '    if (_showSetup && mounted) setState(() { _showSetup = false; });',
            "    final raw = await bind.mainGetLocalOption(key: 'frdp_pending_connect');",
            '    if (raw.isEmpty) {',
            "      if (_pendingPeerId.isNotEmpty && mounted) setState(() { _pendingPeerId = ''; _pendingLabel = ''; });",
            '      return;',
            '    }',
            '    final jd = jsonDecode(raw) as Map<String, dynamic>;',
            "    final pid = (jd['peer_id'] as String?) ?? '';",
            "    var lbl = (jd['machine_name'] as String?) ?? '';",
            "    if (lbl.isEmpty) lbl = pid;",
            '    if (pid.isNotEmpty && mounted) setState(() { _pendingPeerId = pid; _pendingLabel = lbl; });',
            '  }',
            '',
            '  void _connect(BuildContext ctx) async {',
            "    await bind.mainSetLocalOption(key: 'frdp_pending_connect', value: '');",
            '    final pid = _pendingPeerId;',
            "    if (mounted) setState(() { _pendingPeerId = ''; _pendingLabel = ''; });",
            '    if (pid.isEmpty) return;',
            '    connect(ctx, pid);',
            '  }',
            '',
            '  @override',
            '  Widget build(BuildContext context) {',
            '    if (_showSetup) return _buildSetup();',
            '    if (_pendingPeerId.isNotEmpty) return _buildNotification(context);',
            '    return const SizedBox.shrink();',
            '  }',
            '',
            '  Widget _buildSetup() {',
            '    return Container(',
            '      padding: const EdgeInsets.all(10),',
            '      decoration: BoxDecoration(',
            '        color: const Color(0xFF1A1A2E),',
            '        borderRadius: BorderRadius.circular(6),',
            '        border: Border.all(color: const Color(0xFF2D2D2D)),',
            '      ),',
            '      child: Column(',
            '        crossAxisAlignment: CrossAxisAlignment.start,',
            '        mainAxisSize: MainAxisSize.min,',
            '        children: [',
            "          const Text('Portal Sign In', style: TextStyle(fontSize: 11, fontWeight: FontWeight.w600, color: Color(0xFF94A3B8))),",
            '          const SizedBox(height: 8),',
            "          _credField(_userCtrl, 'Username'),",
            '          const SizedBox(height: 6),',
            "          _credField(_passCtrl, 'Password', obscure: true),",
            '          const SizedBox(height: 6),',
            "          _credField(_totpCtrl, 'TOTP code (if enabled)'),",
            '          if (_authError.isNotEmpty) ...[',
            '            const SizedBox(height: 6),',
            '            Text(_authError, style: const TextStyle(fontSize: 11, color: Color(0xFFEF4444))),',
            '          ],',
            '          const SizedBox(height: 8),',
            '          SizedBox(',
            '            width: double.infinity,',
            '            child: ElevatedButton(',
            '              onPressed: _authing ? null : _auth,',
            '              style: ElevatedButton.styleFrom(',
            '                backgroundColor: const Color(0xFF6366F1),',
            '                foregroundColor: Colors.white,',
            '                padding: const EdgeInsets.symmetric(vertical: 8),',
            '                minimumSize: Size.zero,',
            '                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(6)),',
            '                textStyle: const TextStyle(fontSize: 12),',
            '              ),',
            "              child: Text(_authing ? 'Signing in...' : 'Sign in'),",
            '            ),',
            '          ),',
            '        ],',
            '      ),',
            '    );',
            '  }',
            '',
            '  Widget _credField(TextEditingController ctrl, String hint, {bool obscure = false}) {',
            '    return TextField(',
            '      controller: ctrl,',
            '      obscureText: obscure,',
            '      onSubmitted: (_) => _auth(),',
            '      style: const TextStyle(fontSize: 12, color: Color(0xFFF1F5F9)),',
            '      decoration: InputDecoration(',
            '        hintText: hint,',
            '        hintStyle: const TextStyle(color: Color(0xFF475569), fontSize: 12),',
            '        filled: true,',
            '        fillColor: const Color(0xFF0F0F1A),',
            '        isDense: true,',
            '        contentPadding: const EdgeInsets.symmetric(horizontal: 10, vertical: 8),',
            '        border: OutlineInputBorder(borderRadius: BorderRadius.circular(6), borderSide: const BorderSide(color: Color(0xFF2D2D2D))),',
            '        enabledBorder: OutlineInputBorder(borderRadius: BorderRadius.circular(6), borderSide: const BorderSide(color: Color(0xFF2D2D2D))),',
            '        focusedBorder: OutlineInputBorder(borderRadius: BorderRadius.circular(6), borderSide: const BorderSide(color: Color(0xFF6366F1))),',
            '      ),',
            '    );',
            '  }',
            '',
            '  Widget _buildNotification(BuildContext ctx) {',
            '    return Container(',
            '      padding: const EdgeInsets.all(10),',
            '      decoration: BoxDecoration(',
            '        color: const Color(0xFF1E3A2E),',
            '        borderRadius: BorderRadius.circular(6),',
            '        border: Border.all(color: const Color(0xFF22C55E)),',
            '      ),',
            '      child: Row(',
            '        children: [',
            '          const Icon(Icons.computer, color: Color(0xFF22C55E), size: 16),',
            '          const SizedBox(width: 8),',
            '          Expanded(child: Text(_pendingLabel,',
            '            style: const TextStyle(fontSize: 12, color: Color(0xFFF1F5F9)),',
            '            overflow: TextOverflow.ellipsis,',
            '          )),',
            '          const SizedBox(width: 8),',
            '          ElevatedButton(',
            '            onPressed: () => _connect(ctx),',
            '            style: ElevatedButton.styleFrom(',
            '              backgroundColor: const Color(0xFF22C55E),',
            '              foregroundColor: Colors.white,',
            '              padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),',
            '              minimumSize: Size.zero,',
            '              tapTargetSize: MaterialTapTargetSize.shrinkWrap,',
            '              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(6)),',
            '              textStyle: const TextStyle(fontSize: 11),',
            '            ),',
            "            child: const Text('Connect'),",
            '          ),',
            '        ],',
            '      ),',
            '    );',
            '  }',
            '}'
          ) -join "`n"

          $c = $c + $cls
          $c | Set-Content $f -Encoding utf8
          Write-Host "Injected _FsPortalWidget (portal_client_token + /api/portal/auth)"

      # ── Settings tabs: General + About only ──────────────────────────────
      - name: Configure settings tabs (General, About)
        shell: pwsh
        run: |
          $f = "flutter\lib\desktop\pages\desktop_setting_page.dart"
          $c = Get-Content $f -Raw
          $before = $c
          $startMark = 'static final List<SettingsTabKey> tabKeys = ['
          $startIdx  = $c.IndexOf($startMark)
          if ($startIdx -ge 0) {
            $endIdx = $c.IndexOf('];', $startIdx)
            if ($endIdx -ge 0) {
              $newList = $startMark + "`n    SettingsTabKey.general,`n    SettingsTabKey.about,`n  ];"
              $c = $c.Substring(0, $startIdx) + $newList + $c.Substring($endIdx + 2)
            }
          }
          $c | Set-Content $f -Encoding utf8
          if ($c -eq $before) {
            Write-Host "Warning: tabKeys pattern not matched — tabs unchanged"
          } else {
            Write-Host "desktop_setting_page.dart: settings tabs → General, About"
          }

      # ── Build ─────────────────────────────────────────────────────────────
      - name: Build RustDesk (Flutter + Rust)
        run: python .\build.py --portable --hwcodec --flutter --vram
        env:
          VCPKG_ROOT: C:\vcpkg
          VCPKG_DEFAULT_HOST_TRIPLET: x64-windows-static
          LIBCLANG_PATH: C:\llvm\bin

      # ── Collect and rename output ─────────────────────────────────────────
      - name: Collect output exe
        shell: pwsh
        run: |
          $src  = $null
          $kind = ""

          $portable = Get-ChildItem "rustdesk_portable.exe" -ErrorAction SilentlyContinue
          if ($portable) {
            $src  = $portable.FullName
            $kind = "portable"
          } else {
            $installer = Get-ChildItem "rustdesk-*-install.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
            if ($installer) {
              $src  = $installer.FullName
              $kind = "installer"
            }
          }

          if ($src) {
            Copy-Item $src "Fred_Portal.exe" -Force
            $mb = [math]::Round((Get-Item "Fred_Portal.exe").Length/1MB, 1)
            Write-Host "Collected Fred_Portal.exe ($mb MB) [$kind]"
          } else {
            Write-Error "No output exe found — check build logs above"
          }

      # ── Upload artifact ───────────────────────────────────────────────────
      - name: Upload Fred_Portal.exe
        uses: actions/upload-artifact@v4
        with:
          name: Fred_Portal
          path: Fred_Portal.exe
          retention-days: 90
